<%= simple_form_for @room do |f| %>

	<%= f.input :title %>
	<%= f.input :address %>
	<%= f.input :description %>
	<%= f.input :no_of_rooms %>
	<%= f.input :price_in_pence %>

	<%= f.button :submit %>


<% end %>